<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hypertars.neighborChat.dao.BlockApplicationDAO">

    <insert id="insert" parameterType="com.hypertars.neighborChat.model.BlockApplication" >
        insert into BlockApplication (applicant, bid, txt, accepts, decisions, baTime, status)
        values(#{applicant}, #{bid}, #{txt}, #{accepts}, #{decisions}, now(), true)
    </insert>

    <update id="update" parameterType="com.hypertars.neighborChat.model.BlockApplication" >
        update BlockApplication
        <set>
            <if test="applicant != null and applicant != 0" >applicant = #{applicant},</if>
            <if test="bid != null and bid != 0" >bid = #{bid},</if>
            <if test="txt != null" >txt = #{txt},</if>
            <if test="accepts != null" >accepts = #{accepts},</if>
            <if test="decisions != null" >decisions = #{decisions},</if>
            <if test="status != null" >status = #{status},</if>
            baTime = now()
        </set>
        where applicant = #{applicant} and bid = #{bid}
    </update>

    <select id="selectByUidStatus" resultType="com.hypertars.neighborChat.model.BlockApplication" >
        select * from BlockApplication where applicant = #{applicant} and status = #{status}
    </select>

    <select id="selectByBidStatus" resultType="com.hypertars.neighborChat.model.BlockApplication" >
        select * from BlockApplication where bid = #{bid} and status = #{status}
    </select>

</mapper>
